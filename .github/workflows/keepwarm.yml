name: Keep Supabase warm

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping keepwarm endpoint
        run: curl -sf --max-time 20 https://autoescuela-app.vercel.app/api/cron/keepwarm
